/**
 * Calcular el área de un triángulo:
 * Crea una función flecha que calcule el área de un triángulo dado su base y altura.
 */

let areatriangulo = (base, altura) =>
    {
        return((base * altura)/2);
    };