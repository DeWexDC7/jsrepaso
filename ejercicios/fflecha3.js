/**
 * Determinar si un número es par o impar:
Utiliza una función flecha para determinar si un número dado es par o impar.
 */

let par = (numero) => {
    return numero % 2 === 0 ? 'par' : 'impar';
};